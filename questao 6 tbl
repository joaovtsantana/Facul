A saída será 100. Arrays em C# são tipos por referência, então quando se faz int[] numeros2 = numeros1; não é criado um novo array, apenas numeros2 passa a apontar para o mesmo array na memória que numeros1. Por isso, ao alterar numeros2[0] = 100;, o valor em numeros1[0] também muda. Para evitar isso, é necessário criar uma cópia do array usando Clone(), Array.Copy() ou copiando os elementos manualmente, garantindo que cada array seja um objeto diferente.
